# CAO-OMS — working rules

Dean's Office Operations Management System, Wake Forest University School of Medicine / Advocate Health. Dean-facing. Treat every change as production.

## What this repo is

`oms.html` is the entire application: one file, three inline `<script>` blocks, no build step, no dependencies beyond a CDN SheetJS tag. `index.html` is the sign-in page. GitHub Pages serves both from `main`, so **a push to `main` is a deploy**.

Two sibling repos, both private:

- `CAO-OMS-Gateway` — Vercel functions. Every file under `api/` is a thin wrapper — `login`, `me`, `operation`, `password`, `state`, `users` — and all the logic is in `lib/core.js`, which authenticates as a GitHub App and writes to the data repo. It long ago stopped being only the operation path: it issues and verifies sessions, changes passwords, and manages accounts. It resolves the account against `auth/users.json` on every request behind a short cache, so **the role comes from the file, not from the token**, and disabling or demoting somebody takes effect in about a minute rather than being trusted for the life of a session. It still has no array primitives — it validates and persists, it does not merge.
- `CAO-OMS-Data` — canonical state: `state/oms-state.json`, `state/manifest.json`, `operations/processed/`, `snapshots/`, and `RECOVERY.md`. A consolidator workflow folds operations into the state file.

## Release rules

**Client releases go direct to `main`.** No PR, no branch. The local gate must pass first; CI re-runs it on push; a revert is one commit.

**Anything touching the gateway or `CAO-OMS-Data` goes by PR.** Those have no rollback baseline.

Before pushing `oms.html`:

```bash
node test/smoke.mjs oms.html      # behavior
./scripts/release_gate.sh HEAD~1  # form
```

`./scripts/release.sh <minor> "<summary>"` does the whole release: bumps the badge, writes the revision-log entry, runs smoke, runs the gate, commits, pushes, watches CI. It refuses to push if either check fails. `./scripts/rollback.sh <commit-ish>` restores an older artifact. `./scripts/verify_live.sh <sha256>` polls the deployed site until it serves what you expect.

**No counts are written in this file, deliberately.** How many assertions the suite holds, how many checks the gate runs, how large the artifact is: every one of those was written down once and was wrong within days, because a number in prose is a fact with no owner and nothing fails when it drifts. This file said 268 assertions and 11 checks on the day the suite passed 401 and 12. Run the command and read the total it prints. **Gate check 13 fails if a count is written back into this file**; if you want to say the suite is thorough, say that, and let the command say how thorough.

Every non-cosmetic change **must** bump the version badge, append an `OMS_REV_LOG` entry, and touch the embedded User Guide. The gate enforces all three; that is deliberate.

**Descriptive text must be tied to the code it describes.** Standing instruction. The guide hash check proves the guide *changed*, not that it became *true*, and nothing else tied a tooltip or a guide line to the behavior behind it — so text drifted silently and stayed wrong until somebody read it. In one day that produced a tooltip still instructing the exact action that caused the OMS-003 defect, a control promising a forced password change it could not deliver, and task tooltips listing a field set two revisions out of date.

Gate check 12 (`scripts/text_claims.py`, manifest `test/text-claims.json`) enforces it in both directions:

- `{"text": …, "requires_code": …}` — if the interface says it, the code must do it.
- `{"code": …, "requires_text": …}` — if the code does it, something must describe it.

A third form, added in Rev 41, is the one that matters most:

- `{"code": …, "requires_text": …, "reachable_from": ["openTaskModal", …]}` — the symbol must also appear *inside* each named function.

**Existence is not reachability, and check 12 could not tell the difference.** Rev 34 shipped the attributed-note control on the SOP dialog only, while the User Guide described it in general terms, and check 12 passed. It was not a one-off: orphan any function a binding names — leave the definition exactly where it is and rename every reference to it — and check 12 missed it every time it was tried, while the whole local gate, the smoke suite included, missed most of them. Use `reachable_from` on any claim whose text is unscoped, and name the forms the sentence is promising it on. `symbol` overrides what must be found inside them.

**When you add a control, a field, or a status, add a binding for it.** When you remove one, the binding becomes stale and the gate says so. Text is checked outside `OMS_REV_LOG`, which is history and is never rewritten. `test/text_claims_negative.py` feeds the checker real drift and must keep passing; a gate that only ever passes is the failure this project already had once, in check 4.

Gate check 14 (`scripts/no_control_bytes.py`, negative suite `test/no_control_bytes_negative.py`) rejects a shipped artifact holding a NUL, any other C0 control character except tab and newline, or a carriage return. **A single NUL makes git classify the file as binary, which silently disables the `eol=lf` normalization `.gitattributes` exists to enforce** — on the one file whose SHA-256 is quoted in every release note, checked by `verify_live.sh`, and used as the rollback baseline. Rev 37 shipped one, at runtime harmlessly, and the entire gate passed; it was noticed only because `grep` began reporting `Binary file oms.html matches` on an unrelated command. Write the escape in the source, never the byte.

## Things that will bite you

**`OMS_MAP` drops records with no `id`.** Both sides of the sync diff use it, so a collection whose records lack ids emits **zero operations in every direction** — no create, no update, no delete — while the UI reports success. This is not hypothetical: `gw` (CAO Visibility) and `rob` (the Cadence grid) had never synced, in either direction, until they were migrated on 5 Sept 2026. Rev 15 added a guard that reports Unsynced and names any collection still holding id-less records. **Do not remove it.**

**A save producing no operations used to report "Connected."** That is the shape of the bug above. If you touch `OMS_SYNC_ONCE`, keep the `OMS_UNSYNCABLE()` check on both exit paths.

**A `create` for a collection holding id-less rows appends a duplicate.** The operational instruction is unchanged — do not make it reachable — but the mechanism is worth stating correctly, because the original wording sent a reader to the wrong file. The gateway only queues; `consolidate.py` decides. Its `find_record` matches on `str(item.get("id",""))`, so a create whose id is already present is filed as a **conflict**, not applied twice. The duplicate happens only when the existing rows have no id to match against — which is exactly the `gw` 8→9 probe, and is still reproducible today.

**Never migrate canonical data through the client.** The `gw`/`rob` id migration was a direct, snapshotted commit to `state/oms-state.json`. Going through the operation path would have produced 16 CAO Visibility rows and 14 workstreams. Snapshot first, using the repo's own `snapshots/manifest-before-<UTC>.json` convention.

**One overlay, one `#mbody`.** `openModal()` replaces `innerHTML`, so a second modal destroys the first. Anything needing a form mid-edit must be **inline** — that is why the new-person form lives inside the task dialog.

**Sync confirmation takes 20–30 seconds.** Wait for `syncState` to read **Connected** *and* for `OMS_REVISION` to increment before reloading. A reload during "Sync pending" discards the change you are testing. A fixed sleep is not good enough; poll.

## Running the tooling on Windows

`scripts/release_gate.sh`, `scripts/release.sh`, `scripts/rollback.sh` and `scripts/verify_live.sh` are bash. **They do not run in PowerShell.** Use **Git Bash** (ships with Git for Windows): right-click in the repo folder, *Open Git Bash here*. `test/smoke.mjs` is Node and runs in any shell.

Line endings matter here in a way they usually do not: the gate checksums `oms.html`, and `release.sh` computes SHA-256 of the working-tree file. If `core.autocrlf` rewrites LF to CRLF on checkout, every checksum mismatches and `.sh` files fail in Git Bash with `bad interpreter`. Confirm `git config --get core.autocrlf` is `false` for this repo, and that `oms.html` hashes to the value in the current release note.

**Never build a multi-line script inside a shell heredoc. Write it to a file and run the file.** A heredoc feeds literal text to a command's standard input, and the shell parses that text on the way through. `<<PY` expands `$`, backticks and backslashes before the interpreter ever sees them; `<<'PY'` does not. Python and JavaScript are full of all three characters, so the wrong choice corrupts the script with no error at all.

Worse, the escaping is consumed at *every* layer between the tool call, the shell and the interpreter, and it is not obvious which layer ate what. That is not theoretical: a patch script's anchor was written as a backslash followed by `n`, meaning those two characters, and arrived at Python as a real line break, so it could never match a file that held the two characters. The tell was the assertion message printing the search string across two lines. The same day, a heredoc whose closing marker carried one leading space swallowed the rest of the command and died on an unexpected end of file, twice, because the marker must sit alone at the start of a line. A body containing an apostrophe ends a single-quoted context early for the same family of reasons.

So: anything longer than one line, or containing a quote, a backslash, a `$` or a backtick, is written to a file with the Write tool and then run. The content never passes through shell parsing, what you wrote is what executes, and the file survives to be re-read when the anchor fails anyway. Heredocs are for short, literal, escape-free text and nothing else. This matters most for the patch scripts that edit `oms.html`, which are exactly the case that is long, quoted and backslash-heavy.

## Verifying anything

Never report state from memory or from a register row. Read it live:

- Display artifact: `curl` the raw file, `sha256sum`, check the badge and top rev.
- Canonical state: read `ST`/`OMS_REVISION` in a signed-in browser session — the app requires OMS credentials, held in `sessionStorage`, which die with the tab.
- Repo: `git log`, the Actions run, the served file. Not a summary of them.

**The canonical backlog workbook is a register, and the rule above covers it.** On 6 Sept four rows were handed to Hossam as decisions awaiting him; all four described gaps that had been built weeks earlier, one of them thirteen revisions earlier, and one concealed a defect that had shipped as dead code. The rows were not checked against the running system before being reported. A row's `Evidence Review Date` records that somebody edited the row, not that its claim was tested — the `Verified Against` column records the artifact and canonical revision it was tested against, and **a row without one has not been checked**. Re-scoping a stale row is normal; reporting one as a live decision without reading the artifact is not.

**A probe that ignores its argument reports on the wrong thing.** `scripts/text_claims.py` always checks the repository's own `oms.html`, and until Rev 46 it *silently ignored* a path passed on the command line — so a negative control run against a deliberately broken copy reported the untouched file as passing, and a binding that did not work looked like one that did. It now refuses an argument outright. To check a modified artifact, mirror `scripts/`, `test/` and `oms.html` into a scratch tree and run the copy from there. This shape has bitten the project twice: the other time, `grep -c \r` reported every file as CRLF when Python found no CR bytes at all.

## Conventions

Person ids are `slug(name)` with a `-2`, `-3` suffix on collision, **for records the app creates**. Do not re-derive an id for an existing person: the directory holds ids that predate the current `slug`, and `slug("Clare Il'Giovine")` now returns `clare-il-giovine` while canonical holds `clare-ilgiovine`. Re-deriving would create a second person rather than find the first. New `rob` ids follow the same shape through `omsRobId`. Owner strings must match a directory person; `findPerson` binds only when exactly one person matches, and `ownerAmbiguity` blocks a save that matches two. Display names and email names diverge at Advocate — Maggie/Margaret, Ari/Ariana — so the resolution ladder checks both.

`gw` ids are `gw-<YYYY-MM-DD>`; `rob` ids are `rob-<slug of workstream>`.

**Issue ids are not a single namespace, and they have already collided.** The canonical backlog workbook is the register, but `CAO-OMS-Gateway` also names its branches and code comments `oms-0NN-*` for work that was never registered there — so `OMS-046` means the password-change endpoint in the gateway and something else entirely in the workbook. `OMS-050` to `OMS-059` are treated as consumed by the gateway series and must not be minted in the workbook; new workbook rows start at `OMS-060`. When a revision-log entry cites an id, it means the **workbook's**, because `OMS_REV_LOG` is append-only and cannot be corrected afterwards. See DEC-026.

Names to spell correctly: Terry Hales, Terri Yates, Clare Il'Giovine, Dean Boulware, Maggie Scirica, Ari Ball, Rachel Woodside, Erich Huang. **EB** in canonical data and older documents means Dean Boulware; refer to her as Dean Boulware. The acronym is **CAIR**, and **PCORI** is the Patient-Centered Outcomes Research Institute.

## History worth knowing

**Early revision numbers were renumbered once and do not line up with anything written before 31 Aug 2026.** The display log was resequenced to Rev 1–8: what had been Rev 14 became Rev 2, and what had been Rev 33 became Rev 7. The internal gateway revision was deliberately *not* renumbered, which is why it has always been higher and must never be reconciled to the display build. Rev 10 was calendar source replacement — the Events workbook replaces Ari's records, Key Dates replaces Maggie's, and Tasks survive untouched. Rev 11 was the Events parser hotfix that finds the real header row instead of the instruction row above it.

Rev 13 shipped outside its approved scope and was never QA'd. Rev 14 fixed a Cadence grid defect that corrupted status on every documented edit. Rev 15 hardened the release gate, migrated `gw`/`rob` to id-bearing records, and added the silent-save guard. Rev 16 added owner resolution. Rev 17 added Retired status, free-text categories, saved views and multi-day event spanning; Rev 18 made categories user-editable and fixed a live legend-code collision; Rev 19 fixed the Weekly Brief default week and the notification subject standard; Rev 20 removed en-GB spellings and added a guard; Rev 21 was the sync-path release, quarantined deliberately; Rev 22 unified the vocabulary on "task"; Rev 23 gave tasks a category, sortable columns, a grouped checklist export and a working intake form.

Rev 24 added task dependencies with a Blocked state; Rev 25 editor attribution and edit history, both sized L in the register and actually S because the data already existed and nothing read it; Rev 26 a role-aware User Guide and one identity on every draft; Rev 27 a single source of truth for the email subject line, and the defect that had been hiding behind three copies of it; Rev 28 the sign-in half of the forced password change, shipped inert ahead of the gateway; Rev 29 tooltips and guide text that had stopped being true across eleven revisions. Rev 30 brought sign-in accounts into the Admin Console; **Rev 32 added gate check 12**; Rev 33 unified the category vocabulary across both importers; Rev 34 answered four register items on what already existed rather than on new collections; Rev 35 put the attributed-note control on the task dialog, where notes are actually written.

**Revs 36 to 44 are the sync path. Read them before touching `OMS_SYNC_ONCE`.** Rev 36 made Unsynced heal itself and made a confirmation mean the change is actually there. Rev 37 stopped a save re-sending work already with the gateway, and stopped Connected appearing while any of it was outstanding. Rev 38 gave a new Cadence workstream an id — the difference between a workstream existing and existing only on the machine that typed it. Rev 39 put the reconciler on a backoff schedule, fixing a defect Rev 36 introduced. Rev 41 made a refused save say *which* refusal it was. Rev 44 added field-level merge, so two people editing different fields of one record both survive, and a 60-second idle refresh that adopts other people's work without a reload and refuses to while an edit is unsaved or a dialog is open. Revs 40, 42 and 43 are text-only corrections; **Rev 42 is the one worth reading**, because the gate was enforcing a User Guide that a gateway change had made false four hours after it shipped.

Rev 45 pinned the one external script to a fingerprint. Revs 46 to 49 came out of the architecture fidelity review: the SOP review lookahead was wired to the Dashboard, having shipped in Rev 34 as dead code nothing ever called; two sentences promising an Outlook calendar sync that has never existed were removed; cadence became durable through a workbook re-import instead of being erased by it; and events and SOPs gained the Retired status tasks have had since Rev 17.

**Revs 50 to 55 came out of the Fable 5.1 QA, architecture and security pass of 6 to 7 Sept 2026**; the report and reproductions are in `docs/review-2026-09-06-qa-security/`. Rev 50 fixed four sync-path defects the reproductions in that folder demonstrate: the Rev 44 idle refresh had never run because the boot migration left every collection dirty, the client's own version check made the consolidator's field-level merge unreachable, an edit typed while a save was confirming was overwritten and reported as saved, and one stale record discarded a whole batch. Rev 51 was client security: `esc()` had never escaped quotes, stored links had no scheme check, the initial-password formula was in the public artifact, sign-out left the whole state in `localStorage`. Rev 52 made the Admin Console's Access Level, Deactivate and Delete reach the sign-in account they described, on the back of gateway OMS-063, which added the role action and made a password change end earlier sessions. Rev 53 rebuilt the importer so a re-import keeps a matched event's id, status and end date instead of orphaning every linked task. Rev 54 was thirty-odd correctness and accessibility findings. Rev 55 was three defects only the live QA lane showed, including that Create sign-in for an existing person had always run as a password reset because `openModal` replaced `MC` and lost the mode. On the data side, OPS-038 made a changed field with no base value a conflict, and **OPS-039 made the consolidator start from the tip of `main` and restart instead of rebasing**: a multi-record save is one gateway commit per record, GitHub cancels the pending run in a concurrency group whatever `cancel-in-progress` says, and the rebase conflicted on the state file every time, stranding work for the cron. If a save sits on Unsynced with the banner saying it reached shared OMS, read the consolidator runs before the client.

**Revs 56 to 60 finished that pass.** Rev 56 re-resolved a person record after an await; Rev 57 and 58 gave the Key Dates importer a date-range picker for a year-less sheet and fixed the year scan; Rev 59 stopped a read-only or password-pending refusal arming the pending copy. **Rev 60 changed the shape of a save**, with gateway OMS-064 and data OPS-040: a save with more than one operation is one `POST` carrying `{operations:[...]}`, one inbox file under a batch envelope, one commit and one consolidator run, and `consolidate.py` validates every member before applying any. Single operations and single-operation inbox files are unchanged, and the client falls back to one-at-a-time if the gateway answers the envelope with 400. The pollers ask `GET /api/revision` (the manifest, a few hundred bytes) before they read the state, and the manifest `status` reaches the Connected banner. The gateway's fail-open on an unreadable `auth/users.json` is bounded: reads keep working, writes are refused with 503 after fifteen minutes. If you touch `OMS_SYNC_ONCE`, keep both send paths, and keep `OMS_PEEK_REVISION` returning `null` on anything but a well-formed answer so the state fetch still happens.

**Rev 57 and Rev 58 followed on 7 Sept 2026 from Hossam's decisions on the review.** A Key Dates sheet that states no year is asked for the period it covers, with two calendar pickers, instead of being refused (DEC-027); the four intake sections gained an Edit dialog; `seedPeople` seeds one administrator instead of a stale five-person office; `buildSeed` and its seed calendar arrays are gone; `REV17.md` no longer promises the Outlook sync. Rev 58 fixed the year scan that Rev 57's live test exposed: it read six rows and took a `/2027` out of a date lead as the sheet's year. Also on 7 Sept, `OMS_USERS_JSON` was deleted from the Vercel project after proving `auth/users.json` authoritative, and the gateway was redeployed without it (DEC-030): a missing auth file now fails loudly rather than reverting the roster to the seed.

**Rev 61 and gateway OMS-065 were written under Opus 5 on 7 Sept 2026; Rev 62 and gateway OMS-066 are the Fable 5.1 review of them**, recorded in `docs/review-2026-09-06-qa-security/REPORT.md` §12.4. Rev 61 added bulk status on Tasks, Everyone/Mine on the Dashboard, remembered screen and filters in `localStorage`, Copy on a task, and counters that are buttons; `tasksAtRisk` is deliberately not `tasksDueSoon`, because the reminder path excludes owners with no email and the risk counter must not. OMS-065 raised the scrypt cost and made it travel with each record in `kdf`; a record without one is legacy and is rewritten on its owner's next successful sign-in. Rev 62 fixed the four things the review found: the Mine counter and the list it opened were computed by different functions and disagreed on live data, so the Tasks Owner filter gained a directory-resolved `Mine (owned by you)` value (`OMS_MINE_OWNER`) and the counters send it there; the Mine tooltip claimed the cards below narrow, which they do not; Copy is saved before its dialog opens, so Cancel does not remove it, and the text now says so; and the five-counter row is a `.g5` class inside the same breakpoints as the other grids rather than an inline width that beat them. OMS-066 made `scripts/make-users.mjs` write the `kdf` it derived at, without which every account it minted failed its own verification, and stopped the revocation test racing the clock by demoting with the role action. **Owner strings in canonical hold both long and short forms of the same person** (`Hossam Elsaie` and `Hossam`); anything that filters tasks by person must resolve through `findPerson`, never compare strings, and the smoke suite pins that for Mine.

**Rev 63 added export on every grid and removed the two-year limit on the Key Dates period; Rev 64 fixed the phone layout.** The export reads the same list function the screen reads (`omsTaskRows`, `omsCalItems`, `omsSopRows`) and writes an About sheet carrying the canonical revision; if you add a filter to a screen, add it to that function, never to the renderer alone, or the file and the screen will disagree. The two-year refusal in `omsApplyKeyDatesRange` is gone on Hossam's instruction and a `forbids_code` binding keeps it out. **Responsive checks cannot be run in the connected Chrome tab**, whose window ignores resizing, and the in-app browser has no OMS session and must not be handed one; run them in a local harness that serves the shipped artifact with the gateway URL pointed at a stub answering `/api/me`, `/api/revision` and `/api/state` from a copy of canonical, as §12.5 of the review report describes. Below 600 px the header wraps and form fields are 16 px; keep both if you touch the header CSS.

**Rev 65 added the header search and task templates.** `taskTemplates` is now in `OMS_COLLECTIONS`; a template record is `{id:'tpl-<slug>', name, category, lines:[{title, offsetDays, owner, priority}]}` and binds to an event category. **It is applied in exactly one place**, the new-event branch of `saveModal`, behind the `f_apply_tpl` checkbox; the importers must never call `omsApplyTemplates` or `omsTemplatesFor`, and the smoke suite walks every function with "import" in its name to hold that. The search ranks through `omsSearchRank` and the guide states the rule; change one, change the other. Any new way out of an open dialog goes through `omsLeaveForm`, and the smoke suite (finding A-20) counts those exits.

**Rev 66 put a Content-Security-Policy meta tag on both pages (FAB-08).** If you add an external script host, an image host, or a second API origin, the policy must name it or the page fails silently; the layout harness rewrites `connect-src` alongside the gateway constant, and any new harness must do the same. `'unsafe-inline'` stays because the application is inline script; the guide says so and a binding pins it. **Data OPS-041 retries a failed consolidator run once**, event-driven through `workflow_run`, never retrying a `workflow_dispatch` run; if the cron is ever changed, that file is the other half of DEC-019.

**A control must never destroy itself as you type, and gate check 15 is what holds that.** Rev 67 fixed eight of them: three search boxes that took one letter and then dropped focus to the page body, and five filter controls that returned a keyboard user to the top of the document after every choice. **This had been fixed once already** - Rev 12, for the Calendars box alone - and it came back because the fix was a point repair that nothing bound: no assertion, no text claim and no gate check mentioned focus, so two live instances sat beside it for fifty revisions and a fourth box arrived in Rev 65 with nothing to check it against. Everything now routes through `omsRerenderKeepingCaret`, which restores focus and the caret **synchronously**; a `requestAnimationFrame` restore is refused by the gate, because it loses keystrokes typed inside the gap, never runs while a tab is hidden or throttled, and is stubbed to a no-op by `test/smoke.mjs` so no assertion could exercise it. If you add a control whose handler re-runs the renderer that draws it, route it through the helper and give it an id; the gate names the control and the renderer when you forget. **Its blind spots are `onclick`, indirection more than one level deep, and any way of destroying children other than the five DOM idioms it matches.**

**The search-focus defect is FAB-43 in the workbook, not FAB-40.** The Rev 67 release note calls it FAB-40, which the workbook had already used for batch operations, so the two now collide exactly as DEC-026 describes. `OMS_REV_LOG` is append-only and cannot be corrected, so the note keeps its wording and the workbook keeps FAB-40 for batch operations; DEC-031 records the clash and new FAB ids start at FAB-48. Check the workbook before minting an id.

**A hidden tab fires no frame callbacks.** `document.visibilityState` was `hidden` in both browser surfaces available on 7 Sept 2026, and a `requestAnimationFrame` registered there had still not run after 600 ms while a `setTimeout` had. That made a working fix look broken. Check `visibilityState` before concluding anything about timing, animation or focus from a browser probe.

**Rev 68 changed the calendar filters and the category dialog.** The Out of office chip and its filter are gone: out-of-office entries are ordinary events on Calendars now. The Dashboard's 30-day counter still excludes them on purpose, so bind anything about this to the calendar's own expression, not to the words `EB OOO`. **"Standing" is now "Recurring" everywhere a person can read it** - chip, event form, legend, markers, import preview, export column, guide - which reverses Rev 53's R-13 word choice but keeps its rule of one word for one bit, and now matches the stored field, which has always been `recurring`. The internal `calCadence==='standing'` token is unchanged so saved views still apply; do not "tidy" it. **Categories can be added and deleted from the Categories dialog**, but delete only works on a category nothing uses, because `eventCategories()` rebuilds the list from what records carry and a name still in use simply comes back. **Custom categories live in `localStorage`, not canonical**, so a new one is per-browser until a record uses it; making them shared needs a new synced collection across all three repositories.

**Gate check 15's tokenizer must stay a real state machine.** It shipped in Rev 67 with two faults that made it report on the wrong functions while still passing: a regex containing a quote (`.replace(/'/g,...)`) opened a phantom string, and a nested template literal closed the outer one early. `esc()` measured 63,173 characters instead of 150. Both are pinned by fixtures in `test/no_focus_losing_inputs_negative.py`. If you touch `blank_out`, run that suite: a tokenizer that is wrong here does not fail loudly, it just stops looking where it claims to look.

**The register and the tester script were audited against the artifact on 7 Sept 2026 and both were wrong in ways that read as right.** The Canonical Dashboard is now derived from the backlog sheet, not typed, because it had drifted to 124 issues against 156. The tester script told five people "You do not have to sign in twice" when the sign-in page deliberately makes them, asked them to open a multi-day event when canonical holds none, and promised a two-minute idle refresh when `OMS_WATCH_MS` is sixty seconds. **Validate a checklist against the artifact before it goes to people**, the same way a register row is validated: the script had been written from intent, not from the code. `verify_live.sh` proves the bytes; only reading the running system proves the words.

**The browser tool cannot upload from the Desktop, only from folders the session may read**, so a workbook has to be copied into the scratchpad first. And Outlook's message list does not render while its tab is hidden, so mailbox work needs the Chrome window in front with that tab active; a compose pane can be reached by its own URL, but its Discard dialog also needs the tab visible.

**When QA-ing the live application through a browser tool, keep the OMS tab in the foreground and keep page scripts short.** A background tab throttles timers until a 30-second wait exceeds the tool's timeout, and any `alert()` the app raises freezes script injection until the page is reloaded. Both looked like a hung renderer for an hour on 6 Sept; one of them was a real defect (Rev 54's "own create treated as a conflict") announcing itself through the alert it raised.

The gate that exists now was written because the previous one accepted the bare word `Tasks` as proof the User Guide had been updated.
